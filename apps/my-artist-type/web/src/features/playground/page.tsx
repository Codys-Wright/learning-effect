import { PlaygroundPageComponent } from "@features/quiz/client";

// Main playground page now shows our counter demo
export const PlaygroundPage: React.FC = () => {
  return <PlaygroundPageComponent />;
};
